---
- name: Converge
  hosts: all
  become: true
  vars_files:
    - vars/main.yml

  pre_tasks:
    - include_tasks: java-8.yml

  roles:
    - role: geerlingguy.git
      become: true

    - role: geerlingguy.java
      become: true

    - role: geerlingguy.jenkins
      become: true

    - role: ../../../
