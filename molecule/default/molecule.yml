---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: yaml-lint.yml
platforms:
  - name: "${ARJC_TESTENV_NAME:-debian9}"
    image: "${ARJC_TESTENV_IMAGE:-dankempster/debian-jenkins-ansible:9-0.4}"
    command: ${ARJC_TESTENV_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: ${ARJC_TESTENV_PRIVILEGED:-true}
    pre_build_image: true
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
  test_sequence:
    # - lint
    - destroy
    # - dependency
    - syntax
    - create
    - prepare
    - converge
    # - idempotence
    # - side_effect
    - verify
    - destroy
verifier:
  name: testinfra
  options:
    junit-xml: ${ARJC_BUILD_REPORTS:-../../build/reports}/${ARJC_TESTENV_NAME:-debian9}-testinfra-default.junit.xml
  env:
    TARGET_HOST: "${ARJC_TESTENV_NAME:-debian9}"
  lint:
    name: flake8
